# Private Alpha Outreach Messages

Use these templates for recruiting alpha users across different channels.
Personalize the bracketed sections before sending.

---

## 1. Discord / Community Post (~150 words)

**Target:** MCP communities, Claude Code channels, AI dev Discords

---

Hey all -- been building an MCP tool called **retention.sh** that gives your AI coding agent (Claude Code, Cursor) persistent QA memory across sessions.

The problem: every time you start a new session, your agent has zero context about what it already tested. So it re-explores the same flows, misses the same edge cases, and burns tokens doing it.

retention.sh fixes that. One install command, 60 seconds, and your agent starts catching real bugs -- JS errors, broken links, a11y violations, rendering issues -- and remembers what it found so the next run is faster and cheaper.

Early testers are finding 5-15 real issues in their first crawl. One person caught a production auth bug their test suite missed entirely.

We're running an **invite-only private alpha** right now. Free while we're in alpha, limited spots. You'll have a direct line to shape what we build next.

If you want in, DM me and I'll send the install command.

---

## 2. X/Twitter DM (~80 words)

**Target:** Devs posting about MCP tools, Claude Code workflows, AI-assisted testing

---

Hey [name] -- saw your post about [MCP tools / Claude Code]. Building something you might dig: retention.sh gives your AI agent persistent QA memory so it actually finds real bugs across sessions instead of starting from scratch every time.

60-second install, works with Claude Code and Cursor. Free during our invite-only alpha.

We're keeping it small -- want early users who'll tell us what's broken. Interested?

---

## 3. Reddit Comment (~100 words)

**Target:** r/ClaudeAI, r/cursor, r/programming threads about MCP or AI testing tools

---

This is exactly the gap we built retention.sh to fill. It's an MCP tool that gives Claude Code / Cursor persistent memory for QA -- so your agent doesn't start from zero every session.

Point it at your app and it finds real stuff: JS errors, broken rendering, a11y issues, dead links. First crawl usually surfaces 5-15 legit bugs. Each subsequent run gets cheaper because the agent remembers what it already explored.

60-second install, free during alpha. We're doing an invite-only rollout right now -- DM me if you want early access. We're specifically looking for people who'll give us honest feedback on what sucks.

---

## 4. GitHub Issue / Discussion Comment (~80 words)

**Target:** MCP ecosystem repos, testing tool discussions, Claude Code feature requests

---

We built something related to this -- retention.sh is an MCP tool that gives Claude Code and Cursor persistent QA memory. Your agent crawls your app, finds real bugs (JS errors, broken links, a11y gaps), and remembers findings so future runs are faster and cheaper.

60-second install, free during our invite-only alpha. Looking for devs who'll help shape the tool.

Try it: `curl -sL retention.sh/install.sh | bash` -- or DM me for details.
