## SKILL: Stuck Screen Recovery

If a tap produces no visible change, or no element is interactive:

1. press_button("BACK")
2. If screen still unchanged try swiping back
3. If screen still unchanged → press_button("HOME") → launch_app
4. get_next_trajectory to resume your plan

Rule: One tap attempt per element, ever. Do not retry.
